.DS_Store
Thumbs.db

# Workspace cruft that must never land in the public site repo — the Stop hook

node_modules/
dist/
*.log

# Local tooling and experiments. This repo is served straight to the public from
# its own root, so anything that isn't the site itself stays out of it.
.claude/
skills-lock.json
prototypes/
